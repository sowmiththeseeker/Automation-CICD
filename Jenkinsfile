pipeline {
    stages {
        stage('checkout') {
            //git checkout

        }

        stage('build') {
            // Maven or Gradle
            //mvn clean package
           // dotnet build
            //npm build //angular or nodejs

        }
        stage('test') {
            //mvn test
            //dotnet test
           // npm build

        }
        stage('scan') {
           // mv sonar:sonar //spnarqube //url //credeentials
            //service accounts

        }
        stage('artifactory') {

        }
        stage('dev') {
            //connection to the devlopment server
            //publish -> CLI (AWS CLI, Azure CLI, Plugins)
        }
        stage('stage'){

        }
        stage('prod') {

        }
    }
}